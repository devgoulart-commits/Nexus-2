/* ============================================================
   qrcode.js — carregado logo após qrcode.lib.js.
   A função global `qrcode(...)` já é suficiente para todo o uso
   deste sistema (ver svgQRCodeValidacao em script.js), então este
   arquivo só existe para manter a ordem de carregamento dos
   scripts e ficar disponível para eventuais extensões futuras.
   ============================================================ */
